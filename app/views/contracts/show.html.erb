<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_contract_path(@contract), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @contract.title %></h2>
  <p><%= @contract.party_a %></p>
  <p><%= @contract.party_b %></p>
  <p><%= @contract.amount %></p>
  <p><%= @contract.description %></p>
</div>
