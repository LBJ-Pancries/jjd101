<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_company_path(@company), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @company.title %></h2>
  <p><%= @company.description %></p>
</div>
