<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_project_path(@project), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @project.title %></h2>
  <p><%= @project.description %></p>
</div>
