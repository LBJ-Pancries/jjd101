<table class="table">
  <tr>
    <th>Title</th>
    <th></th>
  </tr>
  <% @projects.each do |project| %>
    <tr>
      <td><%= link_to(project.title, project_path(project))%></td>
      <td><%= project.created_at %></td>
    </tr>
  <% end %>
</table>
