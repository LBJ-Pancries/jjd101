<div class="col-md-12">
  <div class="group">
    <%= link_to("New project", new_project_path, class: "btn btn-primary pull-right")%>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>Title</td>
        <td>Description</td>
        <td>Creator</td>
      </tr>
    </thead>
    <tbody>
      <% @projects.each do |project| %>
      <tr>
        <td>#</td>
        <td><%= link_to(project.title, project_path(project))%></td>
        <td><%= project.description %></td>
        <td><%= project.user.email %></td>
        <td>
          <% if current_user && current_user == project.user %>
            <%= link_to("Edit", edit_project_path(project), class: "btn btn-sm btn-default")%>
            <%= link_to("Delete", project_path(project), class: "btn btn-sm btn-default",
                        method: :delete, data: { confirm: "Are you sure?"} )%>
          <% end %>
        </td>
      </tr>
    </tbody>
      <% end %>
  </table>
</div>
